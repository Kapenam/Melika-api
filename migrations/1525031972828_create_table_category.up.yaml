- args:
    sql: CREATE TABLE "category"("id" bigserial NOT NULL, "name" text NOT NULL, "type"
      text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: category
  type: add_existing_table_or_view
