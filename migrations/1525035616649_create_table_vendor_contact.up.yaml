- args:
    sql: CREATE TABLE "vendor_contact"("id" bigserial NOT NULL, "vendor_id" bigint
      NOT NULL, "first_name" text NOT NULL, "last_name" text NOT NULL, "email" text
      NOT NULL, "phone" text NOT NULL, "created" timestamptz NOT NULL, "last_updated"
      timestamptz NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: vendor_contact
  type: add_existing_table_or_view
